(function () {
    var upload = angular.module('upload');

    upload.controller('UploadController', UploadController);

    UploadController.$inject = ['$scope'];


    //以下是controller的定义
    function UploadController ($scope) {
        
    }
}());